spring:
  config:
    activate:
      on-profile: dev
  # Database Configuration
  datasource:
    url: jdbc:mysql://localhost:3306/ai_music_db?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: 1
    driver-class-name: com.mysql.cj.jdbc.Driver
  # Redis Configuration
  data:
    redis:
      host: 127.0.0.1
      port: 6379
      database: 0
      password: # No password for local dev
      jedis:
        pool:
          max-active: 8
          max-wait: -1ms
          max-idle: 8
          min-idle: 0
  # JPA Configuration
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    open-in-view: false
# Security specific config for dev
security:
  jwt:
    secret: "YourVerySecretKeyForDevelopmentEnvironmentReplaceThisWithARandomLongString"
    expiration-ms: 86400000 # 1 day in milliseconds

# MinIO Configuration for dev
minio:
  endpoint: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin
  bucket: "ai-music-works"

app:
  initial-admin:
    email: admin
    password: admin123
    nickname: Administrator
